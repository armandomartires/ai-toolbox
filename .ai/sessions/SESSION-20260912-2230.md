# SESSION-20260912-2230 — Audit the imported project-migration skill

- Date: 2026-09-12
- Agent/model: opencode (anthropic/claude-opus-5)
- Objective: validate whether the hand-imported
  `skills/project-migration` complies with repo guidelines; document the
  remediation without implementing it.
- Context consulted: AGENTS.md, docs/development/authoring-guide.md,
  docs/operations/runbook.md, docs/registry.md, tests/validate.sh,
  scripts/{install,sync-registry}.sh, skills/_template/,
  .ai/context/CURRENT_STATE.md, .ai/decisions/0002,
  .ai/planning/{ROADMAP,SPRINT-CURRENT,BACKLOG}.md, .ai/templates/,
  git status/log.
- Tasks worked on: none executed. Authored REVIEW-0001, TASK-0001,
  TASK-0002, ADR-0003 (Proposed).
- Decisions (human, this session):
  1. The skill's copy of `ai-project-scaffold.sh` is canonical; the
     byte-identical root `scripts/` copy is authorized for deletion.
  2. `scripts/ai-toolbox-scaffold.sh` is authorized for deletion.
     Both files are untracked — deletion is irreversible.
  3. Extend the skill frontmatter schema to sanction `license` and
     `metadata` rather than stripping them; recorded as ADR-0003.
- Commands and validations: `bash tests/validate.sh` → OK;
  `bash -n` on the scaffold script → OK; `diff` of the two scaffold
  copies → identical; secrets grep → policy prose only.
- Problems: skill is functionally sound but was imported outside the
  component registration procedure — no brief, no registry entry, stale
  CURRENT_STATE. Three code defects found (see REVIEW-0001 findings
  5-7). Registry Skills table still empty; regenerating it is part of
  TASK-0001, not this session, because it must land in the same commit
  as the skill.
- Commit/push: none — planning artifacts only, uncommitted by design.
- Next action: execute TASK-0001.
