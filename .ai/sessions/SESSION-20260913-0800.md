# SESSION-20260913-0800 — Audit and harmonize project-workflow

- Date: 2026-09-13
- Agent/model: opencode (anthropic/claude-sonnet-5)
- Objective: audit the untracked `skills/project-workflow/` import against
  repo guidelines and harmonize it to current requirements (user request).
- Context consulted: AGENTS.md, docs/development/authoring-guide.md,
  ADR-0003, the skill's own SKILL.md and all 7 template files, plus —
  found during the audit, not assumed — a separate repo
  `opencode-customization` (`~/AI Workspaces/opencode-customization`),
  its `.ai/30.ROADMAP.md`, `.ai/20.PLAN.md`, ADR-0017, and the
  `S025_WorkflowHarmonization` sprint's four task briefs.
- Tasks worked on: REVIEW-0002, ADR-0004, TASK-0003 (all done).
- Decisions (human, this session):
  1. `ai-toolbox`'s copy of `project-workflow` becomes canonical,
     immediately, ahead of `opencode-customization`'s own planned `S027`/
     `S028` handover sequencing. That repo's retirement of its copy is
     explicitly out of scope here (ADR-0004).
  2. Any content change to this skill bumps its version — done: `2.1.0`
     (which meant three different files across three locations) → `3.0.0`.
- Commands and validations: `diff -rq` across the three machine-local
  copies of the skill; `bash tests/validate.sh`; `bash
  scripts/sync-registry.sh`; `bash scripts/install.sh link` +
  `readlink`; a functional scaffold-into-throwaway-directory smoke test,
  run twice, with every `reference/*.md` link programmatically resolved
  against the filesystem; secrets grep; `wc -c` against both files'
  byte budgets.
- Problems: the first smoke-test pass found 4 broken relative links,
  introduced while trimming the two index files to their byte budgets
  (a `reference/` or `templates/` path prefix dropped as apparently
  redundant). Fixed and re-verified; both files ended up slightly over
  their round-number byte targets (2089/2048, 3087/3072) as a result —
  accepted, since a working link matters more than 15–41 bytes.
- Commit/push: see `git log --oneline -1` for the current hash; no
  remote configured, nothing to push.
- Next action: TASK-0004 (port first MCP server). Separately,
  `opencode-customization` needs its own follow-up to close or redirect
  `S025_WorkflowHarmonization` now that this repo is canonical — not
  actionable from here.
