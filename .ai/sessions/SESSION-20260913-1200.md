# SESSION-20260913-1200 — Scope the first MCP server; amend the stack rule

- Date: 2026-09-13
- Agent/model: opencode (anthropic/claude-sonnet-5)
- Objective: answer "what would be the 1st MCP server?" — TASK
  (then-numbered 0004) "port first MCP server" was unscoped, no server
  named anywhere in the backlog/roadmap/task list.
- Context consulted: `.ai/tasks/TODO.md`, `.ai/planning/{ROADMAP,BACKLOG,SPRINT-CURRENT}.md`,
  `mcp-servers/_template/`, `AGENTS.md`, and — because this session's own
  connected MCP tools include ansible/proxmox/obsidian — their actual
  live status, checked directly rather than assumed. Also
  `opencode-customization`'s `S023_MCPStack` task briefs and
  `opencode.jsonc`, which already enabled all three servers there.
- Tasks worked on: ADR-0005, TASK-0004 (both done).
- Decisions (human, this session):
  1. Confirmed ansible as the first server to port, after being shown
     that proxmox (missing `numpy` for its router) and obsidian (desktop
     app not running) are both currently non-functional here, while
     ansible's core connection works (only environment-dependent tools
     like `ade_environment_info`/`adt_check_env` fail, which is a setup
     gap, not a broken server).
  2. When the ansible server turned out to be an npm package
     (`@ansible/ansible-mcp-server`), contradicting `AGENTS.md`'s
     Python-only MCP-server rule, chose to amend the rule (ADR-0005)
     rather than write a redundant Python wrapper, pick a different
     server, or vendor npm source.
- Commands and validations: `ansible_ade_environment_info`,
  `ansible_adt_check_env`, `ansible_zen_of_ansible`,
  `ansible_list_available_tools` (confirms not purely read-only —
  `ansible_navigator`/`ade_setup_environment` can execute/write);
  `proxmox_route_tools`, `proxmox_proxmox_api_raw` (both fail);
  `obsidian_obsidian_files` (fails, app not running); `bash
  tests/validate.sh`; grep sweep for stale Python-only MCP-server claims
  across `AGENTS.md`, `docs/`, `.ai/context/` after editing.
- Problems: none blocking. Noted for the next task: ansible's tool
  surface isn't purely read-only, so TASK-0005 (the actual port) will
  need to address `AGENTS.md`'s destructive-capability authorization
  rule, not just the config wiring.
- Commit/push: see `git log --oneline -1` for the current hash; no
  remote configured, nothing to push.
- Next action: TASK-0005 — port ansible using the external-MCP shape
  ADR-0005 just established (config wiring + registry entry, no vendored
  source), re-checking proxmox/obsidian's status first in case either
  became available in the meantime.
