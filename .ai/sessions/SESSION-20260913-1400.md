# SESSION-20260913-1400 — Plan the ansible MCP port (PLAN-0001)

- Date: 2026-09-13
- Agent/model: opencode (anthropic/claude-opus-5)
- Objective: write a detailed, multi-session-safe plan for porting the
  first MCP server. The request named "TASK-0004"; that number now
  belongs to the completed docs/ADR amendment after last session's
  renumbering, so the human confirmed the target was the actual port
  (then TASK-0005).
- Context consulted: `AGENTS.md`, `.ai/README.md`, ADR-0005,
  `CURRENT_STATE.md`, `SPRINT-CURRENT.md`, `ROADMAP.md`, `BACKLOG.md`,
  `PROJECT_MAP.md`, `GLOSSARY.md`, TASK-0004, SESSION-20260913-1200,
  and — because the plan depends on what the scripts actually do rather
  than what the docs say they do — `scripts/sync-registry.sh`,
  `scripts/install.sh`, `tests/validate.sh`, `docs/registry.md`,
  `configs/*/README.md`, `mcp-servers/_template/`. Also the live
  `~/.config/opencode/opencode.jsonc` (a known-working wiring for this
  exact server) and `npm view @ansible/ansible-mcp-server`.
- Tasks worked on: PLAN-0001 (new), TASK-0005 (rewritten scope),
  TASK-0007 (new).
- Decisions (human, this session):
  1. **External-server metadata lives in `mcp-servers/<name>/server.json`.**
     ADR-0005 said `configs/*/README.md` prose; `PROJECT_MAP.md` said
     "external MCP server manifests". Both could not be the source of
     truth — `sync-registry.sh` cannot generate a table row from prose,
     and three client READMEs would hold the same fact three times.
     TASK-0005 carries a clarifying amendment to ADR-0005.
  2. **Ansible's destructive tool surface is authorized**, shipping
     enabled, with disclosure in the manifest and every wiring snippet.
     Recorded in full in TASK-0007 per `AGENTS.md`'s requirement that
     authorization live in the task file.
- Structural finding: "port first MCP server" was still two units of
  work — build the external-server mechanism, then use it. Split into
  TASK-0005 (mechanism) and TASK-0007 (payload), each one session, with
  a green repo at the boundary. Second split of the same original TODO
  line; the first produced TASK-0004.
- Notable: TASK-0005 turns `AGENTS.md`'s prose destructive-capability
  rule into an executable `validate.sh` check (`capabilities.destructive`
  ⇒ `authorization.granted` + an existing `authorization.task` file).
  Both task briefs require the new checks be *seen to fail* on broken
  fixtures — a check that has never failed has not been validated.
- Commands and validations: `ansible_list_available_tools` (10 tools,
  connection live); `proxmox_route_tools` (fails — router still missing
  `numpy`); `obsidian_obsidian_files` (fails — app still not running);
  `npm view @ansible/ansible-mcp-server` (26.6.0, MIT, engines
  `node>=24.0`, vs. local Node v22.23.2 — recorded as a plan risk);
  `npx -y @ansible/ansible-mcp-server --version` (prints usage);
  `bash tests/validate.sh` (OK); `bash scripts/sync-registry.sh`
  (registry byte-identical — no components changed).
- Problems: none. Planning only; no component or script touched.
- Commit/push: see `git log --oneline -1`; no remote configured.
- Next action: execute TASK-0005 (mechanism), then TASK-0007 (port).
