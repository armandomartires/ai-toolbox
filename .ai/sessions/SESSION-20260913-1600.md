# SESSION-20260913-1600 — Execute PLAN-0001: external MCP shape, then port ansible

- Date: 2026-09-13
- Agent/model: opencode (anthropic/claude-opus-5)
- Objective: execute the plan written in SESSION-20260913-1400 —
  TASK-0005 (build the external-MCP-server mechanism) then TASK-0007
  (port `@ansible/ansible-mcp-server` with it).
- Context consulted: PLAN-0001, TASK-0005, TASK-0007, ADR-0005,
  `AGENTS.md`, `scripts/{sync-registry,install}.sh`,
  `tests/validate.sh`, `configs/*/README.md`.
- Tasks worked on: TASK-0005 (done, `0e43f95`), TASK-0007 (done).
- Deviations from plan, and why:
  1. **Dropped the planned `shape` field** from `server.json`. A
     self-declared shape can contradict the directory's contents; shape
     is now derived from which marker file exists. ADR-0005's
     Clarification records it.
  2. **Added `preconditions`** to the schema. Justified by the paper
     check the plan required: proxmox needs its `[router]` extra,
     obsidian needs a running desktop app. Two of three candidates, so
     it is a general field, not an ansible-shaped one.
  3. **Pinned** ansible to `@26.6.0` (the plan left this open). Unpinned
     `npx -y` would let a breaking upstream release land silently between
     sessions; stale pins are visible, silent breakage is not.
- Bug found and fixed: both scripts skipped templates by exact match on
  `mcp-servers/_template`, so the new `_template-external` would have
  been published as a real component — and in fact the committed registry
  was *already* listing `template-mcp-server` as a real MCP server. Both
  loops now use a `_template*` glob. The skills loop still has no skip
  (`template-skill` remains in the registry): pre-existing, not this
  task's file, recorded as a TASK-0006 candidate rather than silently
  widened into scope.
- Notable: `AGENTS.md`'s destructive-capability rule is now executable.
  `validate.sh` requires `capabilities.destructive: true` to carry
  `authorization.granted: true` and an `authorization.task` path that
  resolves to a real file. Every check was observed failing for its own
  expected reason on throwaway fixtures, plus a valid-manifest case to
  prove the gate is not failing unconditionally — a negative-only proof
  would not have shown that. The gate was then bitten once more against
  the *real* ansible manifest.
- Phase 3 verification passed, and mattered: Claude Code was installed
  and confirmed to have no prior ansible entry, so it was a genuinely
  clean target. `claude mcp list` reported **`✔ Connected`**. Verifying
  in this session's own OpenCode instance would have proved only that the
  server works, not that the snippet does. LM Studio stays unverified —
  not installed in WSL — and its snippet says so rather than implying
  coverage.
- Commands and validations: `ansible_list_available_tools` (10 tools,
  unchanged from the authorized table); `proxmox_route_tools` and
  `obsidian_obsidian_daily_path` (both still failing, so ansible remains
  the only candidate); `npm view @ansible/ansible-mcp-server`;
  `npx -y @ansible/ansible-mcp-server@26.6.0` (resolves; warns
  `EBADENGINE` on node 22 and runs anyway — direct evidence for the
  `runtime.declared`/`runtime.tested` split); `bash tests/validate.sh`;
  `bash scripts/sync-registry.sh`; `bash scripts/install.sh link`;
  `claude mcp add/list/remove` in a disposable `/tmp` directory;
  `md5sum` to prove the bite-test restore was byte-identical (`git diff`
  proves nothing for an untracked file).
- Problems: the pwsh shell wrapper cannot parse bash function
  definitions or bare `echo`; worked around by putting multi-step
  fixture proofs in a script file and using `printf`. Residue outside the
  repo: `claude mcp remove` leaves an inert empty `projects` entry in
  `~/.claude.json`; left alone deliberately rather than rewriting the
  user's whole global config to delete one empty key.
- Commit/push: `0e43f95` (TASK-0005) and this session's second commit
  (TASK-0007); no remote configured, nothing to push.
- Next action: TASK-0006 (client config snapshots) — last open task in
  sprint S1.
