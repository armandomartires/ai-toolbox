# Session Index

| Session | Date | Agent | Objective | Tasks | Result |
|---------|------|-------|-----------|-------|--------|
| SESSION-20260912-2230 | 2026-09-12 | opencode | Audit imported project-migration skill for guideline compliance | REVIEW-0001, TASK-0001, TASK-0002, ADR-0003 | Planning docs written; implementation deferred |
| SESSION-20260912-2300 | 2026-09-12 | opencode | Implement TASK-0001 and TASK-0002 (project-migration skill fixes, registration, frontmatter schema) | TASK-0001, TASK-0002 | Both done |
| SESSION-20260913-0800 | 2026-09-13 | opencode | Audit and harmonize project-workflow skill; found and resolved cross-repo provenance issue | REVIEW-0002, ADR-0004, TASK-0003 | Done |
| SESSION-20260913-1200 | 2026-09-13 | opencode | Scope "first MCP server"; found stack rule contradicts only-viable candidate, amended it | ADR-0005, TASK-0004 | Done; TASK-0005 (actual port) next |
| SESSION-20260913-1400 | 2026-09-13 | opencode | Plan the ansible MCP port; resolved manifest-location ambiguity and authorized destructive tools | PLAN-0001, TASK-0005, TASK-0007 | Planned; execution deferred |
| SESSION-20260913-1600 | 2026-09-13 | opencode | Execute PLAN-0001: external MCP manifest shape + executable authorization gate, then port ansible | TASK-0005, TASK-0007 | Both done; verified Connected in clean Claude Code |
| SESSION-20260913-1800 | 2026-09-13 | opencode | Scope and execute TASK-0006: multi-client skill deployment + config snapshots; close sprint S1 | TASK-0006, REVIEW-0003 | Done; S1 complete, OpenCode drift 2.1.0→3.0.0 fixed |
| SESSION-20260913-2000 | 2026-09-13 | opencode | Open sprint S2; ADR-0006 (LM Studio MCP-only, loops authored not ported); author release-check loop | ADR-0006, TASK-0008, TASK-0009 | 0008 done; no loop existed to port |
| SESSION-20260913-2200 | 2026-09-13 | opencode | MCP smoke-test harness; close sprint S2 and Phase 2 | TASK-0009, REVIEW-0004 | Done; S2 + Phase 2 complete, S3 left unscoped pending human |
| SESSION-20260913-2330 | 2026-09-13 | opencode | Sprint S3: ADR-0007 (local git mandatory, remote recommended), registry de-dup, pre-commit hook | ADR-0007, TASK-0011, TASK-0010, REVIEW-0005 | Done; Phase 3 complete, 3 defects caught |
| SESSION-20260914-0100 | 2026-09-13 | opencode | Sprint S4: close every closeable item; skill linter, LICENSE, env-var docs + remote + CI verified, human-action runbook | ADR-0008/0009/0010, TASK-0012…0016, REVIEW-0006 | Done; Phase 4 complete. 2 items were undocumented, not blocked |
| SESSION-20260914-0300 | 2026-09-13 | opencode | Plan **and execute** Phase 5 / sprint S5: the task handover contract, for the skill and this repo | PLAN-0002, ADR-0012, TASK-0020…0023, REVIEW-0007 | S5 complete, skill `3.1.0`. Convention caught a false claim in all 4 tasks; the new check caught none. Cold-start benefit **untested** — one session |
| SESSION-20260914-0330 | 2026-09-14 | opencode | Plan Phase 6 / open sprint S6: an instruct layer for the ansible MCP server. **Record reconstructed 2026-09-15** — this session left none | PLAN-0003, ADR-0014…0016, TASK-0026…0032, B-010…B-013 | S6 opened, planning only. 6 source claims corrected first; 2 were defects in this repo's own wiring. **Never implemented — parked 2026-09-15** |
| SESSION-20260915-1000 | 2026-09-15 | opencode (claude-opus-5) | Park S6; plan Phase 7 / open sprint S7: design + production agent loops, and make `agents/` a real category | PLAN-0004, ADR-0017…0019, TASK-0033…0046, B-014…B-017 | S7 opened, planning only. 8 source claims corrected. Found `agent-tiers` unowned/drifted/never-switched-on, agents not portable across clients, and no ROADMAP Phase 6 |
| SESSION-20260915-1400 | 2026-09-15 | opencode (claude-opus-5) | Ratify ADR-0019 and propagate the status change | ADR-0019 | **Accepted.** Unblocks TASK-0041 and TASK-0044. Found the ADR miscounted its own clauses — said "two", had three; corrected in place, not silently |
| SESSION-20260915-1800 | 2026-09-15 | opencode (claude-opus-5) | Execute TASK-0036: re-verify the per-client agent field mapping against live docs; test unknown-key handling | TASK-0036 | **Done.** Emission confirmed on *new* evidence: a superset file loads in Claude Code and silently drops an OpenCode `permission:` block, leaving Write/Edit/Bash in the pool. 5 of 8 capability terms OpenCode-only; `git-ops`/`shell-runner` unexpressible as CC subagents. 4 ADR-0018 rows corrected. **5 self-corrections**, incl. an absence asserted from too small a doc search |
| SESSION-20260915-2000 | 2026-09-15 | opencode (claude-opus-5) | Revise and ratify ADR-0018 against TASK-0036's evidence; propagate | ADR-0018 | **Accepted.** Mechanism survived, reasoning did not: clause 2 rebased on the observed safety failure, 4 facts corrected in place, **new clause 8 — the emitter refuses, never degrades**, and `git-ops`/`shell-runner` settled as OpenCode-only. Unblocks TASK-0037, now the critical path. Found 2 more ROADMAP drift instances (4th and 5th of that class) |
| SESSION-20260915-2200 | 2026-09-15 | opencode (claude-opus-5) | Execute TASK-0037: `agents/_template/` + the normative agent schema | TASK-0037 | **Done.** `agents/` is defined: 9 rule rows, **9**-term vocabulary. The brief's "exclude one-client terms" rule was **overridden by ADR-0018 clause 8.3** — it would have cut the vocabulary to 3 and dropped every real role's boundary. Added a 9th term; `worktree-only` is **partial**; `color` dropped (zero shared values). **Proved by test** that validate.sh exits 0 on a malformed agent file |
| SESSION-20260916-0000 | 2026-09-15 | opencode (claude-opus-5) | Complete Phase 2 in sequence: registry, gate, emission | TASK-0039, TASK-0038, TASK-0040 | **All done; Phase 2 complete.** Sequenced after finding 3 couplings that made parallel unsafe (2 tasks declared the same file; `_fixture-*` is outside the `_template*` registry skip). A finding travelled 0039→0038 (folded `>-` description passes the gate). **0038's fixture harness was unsound on first run** — all 17 cases failed for the wrong reason. Glob ordering nearly inverted a permission meaning |
| SESSION-20260916-0200 | 2026-09-15 | opencode (claude-opus-5) | Execute TASK-0034: inventory the `agent-tiers` drift | TASK-0034 | **Done, with a BLOCKING finding.** Drift benign: repo copy newer for **both** files from one commit, **no unique fix** installed-side, all 4 model IDs resolve. **But the premise is false** — `agent-tiers` was deliberately **kept** by `opencode-customization` (commit `9bae137`, 2026-09-13, user decision, stated reason, unpulled reopen trigger). ADR-0017 blocked pending a human call; TASK-0035's justification gone. Also freed TASK-0044 of an unnecessary dependency |
| SESSION-20260916-0400 | 2026-09-15 | opencode (claude-opus-5) | Execute the human's decision: `agent-tiers` stays with `opencode-customization` | ADR-0017, TASK-0035, TASK-0045 | **ADR-0017 REJECTED** — this repo's first; title changed since the old one asserted the rejected claim. 3-condition reopen trigger added. TASK-0035 **cancelled** (premise gone, not pending); TASK-0045 **rescoped** to author rather than import. The `{tier:}` resolver gap **documented** in 3 places. Fixed a dangling `skills/agent-tiers/` path in the shipped design-brief loop, plus 3 stale claims found by grep |
| SESSION-20260916-0600 | 2026-09-15 | opencode (claude-opus-5) | Author the design roles; extend the capability vocabulary | TASK-0043 (+ amendments to 0037/0038/0040) | **Done. Phase 2 exercised end to end.** 3 roles authored; gate passes on real content; registry populates; 6 files emitted and inspected; **`critic` proved read-only at runtime in both clients**. Step-2 gate found the vocabulary missing a delegation-allowlist term — **escalated**, then added as a 10th term via follow-ups in definition→enforcement→emission order. `design-doc-writer` **declined** on grep evidence; role count 6 not 7 |
| SESSION-20260916-0800 | 2026-09-15 | opencode (claude-opus-5) | Author `loops/project-build/` — the production sequence | TASK-0044 | **Done.** 8 steps, 218 lines, source read **in place** (no import happened). Three unforecast findings: ADR-0019 needs a **`document` step** the source lacks (7 items, 0 doc mentions); separating the two bounds revealed the **review path was unbounded**, so a third bound was added; and the two-owners question had **no answer among the brief's options**, both assuming this repo can edit another repo's skill. Gate proven to cover the file |
| SESSION-20260916-1000 | 2026-09-15 | opencode (claude-opus-5) | Author the production roles; fix two emitter defects | TASK-0045 (+ amendments to 0037/0038/0040) | **Done. Phases 1-4 complete.** 3 roles authored, all **OpenCode-only**; 9 files emitted. **Two emitter defects found by diffing emitted output against the references fact by fact**: alphabetical glob order downgraded `git push --force` from **deny to ask**, and `no-force-push` omitted `git clean -f*`. Both would have passed a read-through. `bash-allowlist` also needed parameterising. `shell-runner` declined. **Only the pilot remains** |
| SESSION-20260916-2100 | 2026-09-16 | opencode (claude-opus-5) | Plan sprint S8: three human-requested third-party "plugins" (ponytail, omniroute, graphify) | ADR-0021, PLAN-0005, TASK-0048…0051, SPRINT-S8, B-019, B-020 | **Planned, nothing implemented** (explicit instruction). **The request's central word was the wrong abstraction**: "plugin" names three unrelated mechanisms, so no `plugins/` category — ADR-0016's reasoning extended, its 3 plumbing claims **re-verified**. graphify → `mcp-servers/` external shape; ponytail → `configs/` docs only (its `ponytail-mcp` is **unpublished**, so ADR-0005's shape has no launch command); omniroute → **out of the component layer** (human decision). **Two upstream claims falsified before the sprint began, both by reading source not READMEs** — graphify's README contradicts its own `src/cli.ts`, and `serve.ts:188-195` shows `graphify serve` exiting 1 with no graph, which would make `smoke-mcp.sh` report FAIL where the truth is SKIP (**B-020**, latent for any stateful server). S7 left open — `REVIEW-0008` missing — so S8 is **not** promoted to SPRINT-CURRENT |
| SESSION-20260916-2300 | 2026-09-16 | opencode (claude-opus-5) | Close sprint S7 with the missing `REVIEW-0008` checkpoint; archive S7 and promote S8 | REVIEW-0008, B-021 | **S7 CLOSED (approve).** The pre-committed question *did anything get exercised?* is **YES**, verified from artifacts not task logs: gate rejects a broken real role, 9 files emitted across 2 clients with the 3 OpenCode-only roles **skipped not degraded**, lock is a dedicated commit, pilot's checker exits 0 and 1 correctly. **3 findings:** `validate.sh` left its sub-second budget (622→960 ms across S7, ~1150 ms now); **4 tasks incl. the pilot left no session record** (not reconstructed — and `INDEX.md` row-count matches file-count, so no check could see it); `qa-test` **cannot run tests** while its description says it does → **B-021**. **4 files disagreed about S7's own state** (TASK-0033 `planned` in the sprint table vs `done` in its file; "six tasks" vs 13; roadmap "eleven"; B-015…B-017 still `ready` after delivery) — all swept in the closing commit. **My first timing measurement was wrong in the reassuring direction**: `/tmp` ext4 vs `/mnt/c` 9p understated by ~40% |
| SESSION-20260917-0100 | 2026-09-16 | opencode (claude-opus-5) | Act on "work on S6 complete implementation": report state, then un-park S6 and re-queue S8 | TASK-0052 | **S6 is CURRENT again; S8 re-queued** (3rd sprint state: promoted then un-promoted before any work, so no checkpoint). **S6 could not be completed in one session and saying so was the first output** — all three ADRs are **empty skeletons** ("to be written" in every section) and TASK-0031 depends on ADR-0016; ratification is a human act. **Four defects found in S6s own plan before executing it**, all on the guard: **D1** it matched PVE-class by *group name* while the real playbook uses `hosts: sigsrvpve1`, a **bare hostname**; **D2** all five fixtures were satisfiable by a guard that classifies **nothing**, so a fully green run would have proven nothing → **lesson 8s third instance, in a fixture set not a check**; **D3** matching the `module_defaults` key rather than its `setup` entry over-accepts the real playbook; **D4** `ansible-lint` is at `~/.venvs/sigma-ansible/bin/`, not in the target repo, not on PATH. **Common cause: the plan was written from `ansible.cfg`s prose and never opened the playbook the guard must classify — the hazard was verified, the subject was not.** Sprint table also disagreed with its own task files on first read (0029/0030 `planned` vs `done`). Both swap directions **free**, confirming TASK-0033s prediction from the other side. SIGMA `git status` clean; `validate.sh: OK`; no registry diff |
| SESSION-20260917-0200 | 2026-09-16 | opencode (claude-opus-5) | Run S6's two spikes; write all three ADR bodies from what they observed | TASK-0027, TASK-0028, ADR-0014, ADR-0015, ADR-0016 | **Both spikes done; all 3 ADR bodies written and left `Proposed`** (ratification is the human's). **Both spikes inverted their own briefs.** TASK-0027: the target repo's gate **passes** on real content for the first time on record (0 failures / **53 rules** / exit 0 — live but unproven since S004.T007); the first run failed exit 2 on **copy-artifacts** (uncopied `vault_password_file`); the fidelity limit has a **second** reason the brief lacked — the clean pass required *editing* `ansible.cfg`; and `ansible-lint` **writes `ansible.log` with no flag**, vindicating copy-not-in-place by mtime rather than by caution. **The custom-rule probe needed 4 runs**: run 1 was silent at exit 0, which alone reads as "custom rules don't work" and would have forced `pre-commit` for nothing — `-L` showed it **loaded** (53 rules → 54) and `-c /dev/null` showed it **firing**; the cause was `profile: production` filtering rules not in `enable_list`. TASK-0028: interception **works in BOTH clients** — the opposite of the expected "no" — Claude Code **documented**, OpenCode **observed live** (a probe blocked `ansible_zen_of_ansible`, then was reverted and the revert verified two ways). **Decisive finding: the clients disagree on the tool's NAME** (`mcp__ansible__zen_of_ansible` vs `ansible_zen_of_ansible`), so ADR-0016 declines the category **by the opposite route** from the plan's. **ADR-0015 clause 1 formally reversed** (symlink deploy refutes fill-in-place; `templates/` survives for copy-out) and **two ADRs retitled**, filenames kept per ADR-0017. **Three mechanisms in this sprint can each be installed and inert** → TASK-0031 now owes a **fires-proof**. Also swept stale statuses in files corrected hours earlier — the same disagree-class, in the same session. SIGMA untouched; `validate.sh: OK`; no registry diff |
| SESSION-20260917-0400 | 2026-09-16 | opencode (claude-opus-5) | Narrow the ansible MCP blast radius; build and prove the `gather_subset` guard | TASK-0026, TASK-0031 | **S6's highest-value deliverable EXISTS and is PROVEN TO FIRE. Closes B-011, B-012, B-013** — the first S6 items resolved by S6's own execution. All five Phase 6 exit criteria now met. TASK-0026: the false blast-radius claim was in **six** places, not the four its brief predicted (extras: `docs/operations/runbook.md` and a *lessons* list — the two most quotable places to be wrong), three Inputs rows were stale and one named a renamed file, and **one acceptance criterion was DECLINED** because `ADR-0020` had already refuted it. Gate's authorization check **observed failing** for the first time against this manifest, then restored byte-identically by SHA-256. TASK-0031: a custom `ansible-lint` rule, **10/10** in its own fires-proof including a negative control reproducing the `enable_list` silent-no-op trap. **The decisive evidence is OUTSIDE the fixture set**: the real playbook flipped to `gather_facts: true` in a `/tmp` copy fired naming `sigsrvpve1` resolved through the real nested inventory — which is what makes its silence on the unmodified file discriminating rather than inert (TASK-0052's D2). **Three defects found en route, two mine:** (a) **TASK-0027's recommendation was WRONG on its own tiebreaker** — per-rule config in `.ansible-lint` is FATAL for a custom rule (`additionalProperties: false`, exit 3), so `get_config()` is reachable only by an illegal config → env vars, downgrade recorded; (b) **fixture 4 could never reach its own branch** (`syntax-check` is unskippable and fails an undefined-variable target first) — a branch unreachable by its test case is not proven by a passing suite; (c) **my harness matched the rule ID, which appears in ansible-lint's ERROR text**, so four fixtures read as "fired but wrong message" when nothing had run. Also **corrected a shipped false claim I created in the same change** (`hazards.md` said the repo ships no linting rule) and **retracted a false claim from the previous session** (`lineno=` was valid all along; one cause reported as two). Gate unaffected: 1111/1108/1073 ms vs ~1150 ms. SIGMA untouched — every mutation in `/tmp` |
