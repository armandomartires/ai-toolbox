# SESSION-20260915-1400 — Ratify ADR-0019

- Date: 2026-09-15
- Agent/model: opencode (anthropic/claude-opus-5)
- Objective: Ratify ADR-0019 (design convergence is human acceptance;
  autonomy stops at the merge gate), and propagate the status change to
  every artifact that tracks it.
- Context consulted:
  `.ai/decisions/0019-design-convergence-and-the-autonomy-boundary.md` in
  full; the `## Status` line of all 19 ADRs, to match the accepted-status
  format; `.ai/planning/SPRINT-CURRENT.md`; `.ai/tasks/TODO.md`;
  `.ai/context/CURRENT_STATE.md`;
  `.ai/planning/plans/PLAN-0004-design-and-production-agent-loops.md`;
  `.ai/tasks/TASK-0041-author-design-brief-loop.md`;
  `.ai/tasks/TASK-0044-author-project-build-loop.md`.
- Tasks worked on: none. This session changes a decision's status and the
  artifacts that reference it; it is not a task brief's execution.
  Deliberately **not** folded into TASK-0033, which is `done` with a
  recorded commit hash — appending to a closed task's log would make its
  handover table describe a state it did not produce.
- Decisions: **ADR-0019 accepted** by the human, on the date it was
  proposed. It needed ratification rather than evidence, because it
  *narrows a stated requirement* — the delivered system is autonomous
  through implementation, testing and documentation, with a mandatory human
  gate before merge and push, which is narrower than the "no human
  intervention" originally asked for.
- Commands and validations: `grep` across `.ai/` for every ADR-0019
  status reference, to find all propagation points rather than assuming
  which files carried them. `bash tests/validate.sh` → `OK`, run twice
  (manually and by the pre-commit hook). `bash scripts/sync-registry.sh`
  → **no diff**, correct for a decision-only change.
- Problems: **The ADR miscounted its own clauses.** Its Status section and
  its Decision heading both said "two clauses" while the body contained
  **three** — clause 3, rejecting dynamic workflows, was added during
  drafting on 2026-09-15 and the count was never updated. Corrected in
  place at ratification, with the correction recorded in the Status
  section rather than made silently, because a decision that miscounts its
  own clauses invites a reader to treat the third as commentary rather than
  normative. It is normative: clause 3 is the reason both S7 loops are
  built on portable primitives at all.

  This is **lesson 7 in miniature** — a claim decaying between being
  written and being acted on, inside a single document, over four days,
  found only by reading the file rather than trusting the plan's summary of
  it. Worth noting that `PLAN-0004` and `SPRINT-CURRENT.md` both described
  the ADR's content correctly; only the ADR's own self-description was
  wrong.
- Commit/push: see below.
- Next action: **Three S7 tasks are now startable, on two independent
  fronts.** `TASK-0041` (`loops/design-brief/`) is unblocked and depends on
  no spike — it is the natural next step, and it must decide the **lock
  mechanism** that ADR-0019 deliberately left open. `TASK-0034` and
  `TASK-0036` remain independent spikes. `TASK-0044` is unblocked by this
  ADR but still waits on `TASK-0035`'s import.
