# SESSION-20260913-1800 — TASK-0006: multi-client deployment; close sprint S1

- Date: 2026-09-13
- Agent/model: opencode (anthropic/claude-opus-5)
- Objective: proceed with the last open task in sprint S1. TASK-0006 had
  no brief, so it was scoped from investigation first, then executed.
- Context consulted: `AGENTS.md`, `ROADMAP.md`, `SPRINT-CURRENT.md`,
  `CURRENT_STATE.md`, ADR-0002, ADR-0004, `docs/operations/runbook.md`,
  `scripts/{install,sync-registry}.sh`, `tests/validate.sh`, and the live
  client state: `~/.claude/skills/`, `~/.config/opencode/skills/`,
  `/mnt/c/Users/armando.martires/.lmstudio/`.
- Tasks worked on: TASK-0006 (brief written, then done), REVIEW-0003
  (sprint checkpoint).
- Investigation findings that shaped the scope (all pre-existing):
  1. `install.sh` was hard-coded to `~/.claude/skills` despite
     `AGENTS.md` claiming portability across three clients.
  2. **Consequence: OpenCode was running `project-workflow` 2.1.0** — a
     hand-placed copy — while the repo shipped 3.0.0. A live correctness
     bug, not a missing feature; nothing in the repo could ever update it.
  3. The registry listed `template-skill` as a real component (the skills
     half of the leak TASK-0005 fixed for MCP).
  4. LM Studio *is* installed (Windows-side) with `{"mcpServers": {}}` —
     a clean target, and a way to close TASK-0007's "unverified" label.
  5. Dismissed as a non-defect: `~/.claude/skills` symlinks point at
     `~/AI_Workspaces` (underscore) while the repo path has a space.
     `~/AI_Workspaces` is a symlink to the same directory — identical
     inode confirmed — so they resolve.
- Human decisions (this session): scope = skills deploy + config
  snapshots (not config-only, because of finding 2; not full Phase 2 exit,
  because LM Studio skill support does not exist); overwrite policy =
  replace only repo-owned skill names, announce it, never touch others.
- Bugs found *during* execution, both in code written earlier this sprint:
  1. **`ln -sfn` against a real directory creates the link inside it.**
     The first OpenCode deploy printed its NOTICE, exited 0, and left the
     stale 2.1.0 `SKILL.md` live with a nested
     `project-workflow/project-workflow` symlink. Caught only because the
     criteria demanded checking the deployed *version* rather than the
     script's own output. Fixed with an explicit `rm -rf` before linking.
  2. **Client name mismatch** — the new table said `claude`, the config
     dir is `claude-code`. The new pairing check caught it on its first
     run, pre-commit, which is exactly why it was added.
- Notable: the through-line of this whole sprint is that **a script
  reporting success is not evidence the effect happened.** Three of the
  sprint's defects (template leak, `ln -sfn`, name mismatch) were
  invisible to any check that trusted exit codes.
- Commands and validations: `bash tests/validate.sh`;
  `bash scripts/sync-registry.sh` (one line removed — the template row);
  `install.sh` with `link`, `copy`, `--client opencode`,
  `--client claude-code`, `--client bogus` (exit 2), `--help`, and three
  consecutive runs for idempotence; `readlink -f` + `grep version` through
  the OpenCode link (`3.0.0`, was `2.1.0`); `ls` confirming `agent-tiers`
  survived; fails-when-broken proof of the pairing check for both clients;
  LM Studio — entry written into the live `mcp.json`, `python3 -m
  json.tool` parse, restore verified byte-identical by md5
  (`1a6618c9486afd375cdf1135b5b2acc4`), and a real MCP `initialize`
  handshake returning `serverInfo: {"name": "ansible-mcp-server"}`.
- Problems: the pwsh wrapper cannot parse bash function definitions, bare
  `echo`, or inline `VAR=x cmd` env assignment; worked around by putting
  multi-step proofs in script files under `/tmp/opencode`. `copy` mode was
  exercised on Claude Code and then reverted to `link`, since leaving real
  copies there would contradict ADR-0002.
- Honest gaps, recorded not glossed: LM Studio's ansible server is
  unverified in the app's own UI (needs the GUI); LM Studio has no Agent
  Skills target, so Phase 2's "one skill working in all clients" exit
  criterion is unmeetable as written; and `loops/` is still empty although
  the S1 objective named loops. All three are follow-ups in REVIEW-0003.
- Commit/push: this session's commit closes S1; no remote configured,
  nothing to push.
- Next action: open a Phase 2 sprint. Start from REVIEW-0003's follow-ups
  — re-scope the Phase 2 exit criterion, decide whether loops belong in
  Foundation, and pick up backlog B-003 (MCP smoke-test harness), now
  unblocked because a real server exists to test.
