# Active Plan

What is actively in flight, right now. This file is volatile — it
reflects *today*, not history (that's `30.ROADMAP.md`'s sprint table and
`tasks/`) and not the future (that's `30.ROADMAP.md`'s Next Up).

**Size budget**: keep this file small. "Recently completed" holds at
most the three most recently closed sprints in full prose — collapse
older entries to a one-line row linking to a checkpoint or task brief
(see `reference/size-budgets.md` for the full rule and why it exists).

Last updated: YYYY-MM-DD.

## Current sprint

**`S###_SprintName`** — one sentence on what this sprint is for.

## In flight

| Task | Status | Notes |
|---|---|---|
| `S###.T###_Name` | not started / in progress / blocked | |

## Blocked

Anything blocked, and on what. "Nothing currently blocked" if true — say
so explicitly rather than leaving the section empty and ambiguous.

## Recently completed

| Task | Sprint | What |
|---|---|---|
| | | |

## Next up (not yet started)

See `30.ROADMAP.md` → "Next Up" for the prioritized list. Nothing is
committed to starting next until a task file exists for it.
